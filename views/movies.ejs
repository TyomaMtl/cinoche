<!DOCTYPE html>
<html>
    <head>
        <title>Movies</title>
        <meta charset="utf-8">
    </head>
    <body>
        <h1>Movies</h1>
        <ul>
            <% movies.forEach((movie) => { %> 
                <li><%= movie.title %></li>
            <% }) %> 
        </ul>
    </body>
</html>