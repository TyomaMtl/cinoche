<!DOCTYPE html>
<html>
    <head>
        <title>Genres</title>
    </head>
    <body>
        <h1>Genres</h1>
        <ul>
            <% genres.forEach((genre) => { %> 
                <li><%= genre.name %></li>
            <% }) %>
        </ul>
    </body>
</html>